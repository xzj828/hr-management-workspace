# 西鸣人事管理系统视觉设计标准

本文件以现有考勤模块为视觉真值，并将同一套桌面企业后台语言应用到招聘模块。目标是清晰、克制、可信：信息多但层级少，业务动作明确但不制造按钮墙。

## 1. 设计原则

1. 一个页面只突出一个主任务和一个高强调主动作。
2. 用留白、字号和分割线建立层级；卡片只用于真正独立的业务分组，不把每段说明、每个字段或每行数据都做成卡片。
3. 页头负责“我在哪里/能做什么”，工具栏负责筛选，主面板负责数据；不要叠加 Hero 卡、Tab 卡、Section 卡、Item 卡四层容器。
4. 高频动作直接显示；编辑、归档、删除等低频动作使用文本按钮或行级菜单。危险动作只在确认弹窗中使用红色实心按钮。
5. 技术状态渐进披露。业务 HR 首屏看到岗位、状态、结果和下一步；端口、目录、Worker 日志只在诊断或详情中出现。
6. 状态不只靠颜色表达；任何空态、错误态、加载态都保留页面骨架与可恢复动作。

## 2. 颜色

- 页面背景：`#f3f6f8`
- 主表面：`#ffffff`
- 主文字：`#0f172a`
- 次文字：`#334155`
- 弱文字：`#64748b`
- 分割线：`#e2e8f0`
- 品牌/成功：`#0f9f8f`，深色 `#087f73`
- 警告：`#d97706`
- 危险：`#dc4a4a`，危险按钮 `#b83f4b`
- 导航深色：`#101827`

同一屏内不新增第二套品牌绿。大面积彩色背景只用于一个关键 KPI 或小范围状态提示。

## 3. 字体与层级

- 字体：`Inter, PingFang SC, Microsoft YaHei, system-ui`
- 页面标题：27px / 700，紧凑字距 `-0.025em`
- 区块标题：15–20px / 700
- 正文：13px / 400–600，行高 1.6
- 表格与控件：12–13px
- 辅助文字：10–11px
- Eyebrow / Kicker：9–10px / 800 / 大写 / 0.11–0.15em 字距
- 数字 KPI：26–31px，可使用常规无衬线；招聘结果不单独引入 Georgia。

## 4. 间距与布局

- 基础单位：4px
- 常用间距：8 / 12 / 16 / 22 / 28 / 34px
- 页面容器：最大 1660px，左右 34px，上 28px，下 42px
- 页面栈间距：22px；同一区块内部 8–16px
- 桌面优先使用单列主流程；只有并列信息确有比较价值时使用 2 列，避免同屏四列功能卡。
- 表格和列表默认占满主面板；筛选工具栏单行，窄屏再换行。

## 5. 圆角、边框与阴影

- 按钮/输入：9px
- 主面板：15px
- 小状态/标签：6–9px 或胶囊
- 标准边框：1px `#e2e8f0`
- 标准阴影：`0 1px 2px rgba(15,23,42,.025)`
- 浮层阴影可更明显；普通业务卡片禁止大投影或悬浮变换。

## 6. 组件

### 页面头

标题、简短说明、一个主动作。页头本身不放白色卡片背景。

### 面板

一个逻辑区域一个面板。面板内部使用标题行、分隔线、表格/列表；不要在面板里继续嵌套同样边框的卡片。

### 按钮

- 主按钮：深墨色或品牌色实心，一页最多一个。
- 次按钮：白底描边。
- 行动作：文本按钮。
- 同一对象同屏最多一个实心按钮，其余降级。
- 禁用态必须保留原因或 title，不仅降低透明度。

### 数据列表

列表行高度 48–64px；主信息左对齐，状态居中，动作右对齐。用分隔线替代逐行卡片。

### 状态与通知

正常状态用小圆点/文字；警告和错误使用浅色 inline notice。全局错误只用于全页失败，局部失败贴近对应区域。

### 弹窗与抽屉

必须 `role=dialog`、`aria-modal`、标题关联、焦点圈、Escape 与焦点返还。破坏性请求处理中不可关闭。

## 7. 招聘页面约束

- 招聘看板：采用考勤看板的“过滤/页头/KPI/两列图表或列表”节奏，不额外造入口卡片墙。
- 招聘作业台：使用同一路由三页向导；顶部步骤条持续表达位置，当前页只显示“岗位与账号 / 招聘标准 / 执行方案”之一。文件页使用考勤式虚线拖拽区，执行摘要和唯一主按钮只在最终页出现。
- 结果中心：筛选工具栏 + KPI + 视图切换 + 单一结果面板；候选人、运行、事项使用紧凑列表。
- 管理后台：子导航为文本 Tab；每个子页使用一个主面板。账号、流程、模型改为列表行，技术详情折叠；页内不再使用卡片网格。

## 8. 响应式与动效

- 1050px 以下双列收为单列；720px 以下工具栏、操作组纵向堆叠。
- 表格可局部横向滚动，但页面本身不能横向溢出。
- 动效限于 150–220ms 的颜色、边框和轻微透明度变化；不对普通卡片做上浮。
- `prefers-reduced-motion` 下关闭骨架之外的装饰动效。

## 9. 禁止项

- 禁止同屏多个高强调主按钮。
- 禁止 Hero、Tab、Section、Item 四层卡片嵌套。
- 禁止用英文副标题制造无业务价值的视觉噪音；Eyebrow 只保留在页面级或真正需要区分语境的区块。
- 禁止小于 10px 的业务正文或 8px 的可交互文字。
- 禁止只为“好看”新增渐变、玻璃效果、夸张阴影或装饰图形。
